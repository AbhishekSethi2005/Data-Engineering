# Evidence for the Delta MERGE

Run the MERGE cell in `notebooks/delta_scd_assignment.ipynb` and save a screenshot showing the successful completion message as `merge_completed.png` in this directory.

The image records the incremental load: existing customer identifiers are refreshed and new identifiers are inserted through a Type 1 Delta MERGE.
